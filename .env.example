# ========================================================================
# FICHIER DE CONFIGURATION - EXEMPLE
# ========================================================================
# Ce fichier est un modèle pour votre fichier .env
# Copiez ce fichier en ".env" et remplacez les valeurs par les vôtres
# NE JAMAIS COMMITER LE FICHIER .env SUR GIT (il contient vos secrets)
# ========================================================================

# ========================================================================
# SÉCURITÉ - SECRETS (OBLIGATOIRES EN PRODUCTION)
# ========================================================================
# Générez des secrets forts avec :
#   python -c "import secrets; print(secrets.token_hex(32))"
#
# IMPORTANT : Ces valeurs sont OBLIGATOIRES en production
# L'application refusera de démarrer sans elles

JWT_SECRET_KEY=CHANGEZ_MOI_GENEREZ_UN_SECRET_FORT_DE_64_CARACTERES_MINIMUM
FLASK_SECRET_KEY=CHANGEZ_MOI_GENEREZ_UN_SECRET_FORT_DE_64_CARACTERES_MINIMUM

# Algorithme JWT (ne pas changer sauf si vous savez ce que vous faites)
JWT_ALGORITHM=HS256

# Durée de validité des tokens
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ========================================================================
# ENVIRONNEMENT
# ========================================================================
# Valeurs possibles : development, production
# En production, les secrets faibles seront REFUSÉS
FLASK_ENV=development
FLASK_DEBUG=False

# ========================================================================
# CORS - Domaines autorisés (séparés par des virgules)
# ========================================================================
# En développement : http://localhost:5173
# En production : https://votredomaine.com
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ========================================================================
# RATE LIMITING (Protection anti-DDoS)
# ========================================================================
# Storage pour le rate limiting
# memory:// pour dev, redis://localhost:6379 pour prod
RATE_LIMIT_STORAGE_URL=memory://

# Limite par défaut si non spécifié sur une route
RATE_LIMIT_DEFAULT=100 per hour

# ========================================================================
# HTTPS / SÉCURITÉ
# ========================================================================
# Activer uniquement si vous avez un certificat SSL valide
ENABLE_HTTPS=False
FORCE_HTTPS=False

# ========================================================================
# LOGGING
# ========================================================================
# Niveaux : DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
LOG_FILE=logs/security.log

# ========================================================================
# BASE DE DONNÉES (optionnel, actuellement utilise JSON)
# ========================================================================
# Pour une migration future vers PostgreSQL/MySQL
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ========================================================================
# NOTES DE SÉCURITÉ
# ========================================================================
# 1. Ne JAMAIS commiter le fichier .env sur Git
# 2. Générer des secrets DIFFÉRENTS pour dev et prod
# 3. Changer les secrets régulièrement (tous les 3-6 mois minimum)
# 4. En production, utiliser un gestionnaire de secrets (AWS Secrets Manager, etc.)
# 5. Ne jamais partager vos secrets par email ou chat
